{
  "name": "regalops-project-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && node -e \"const fs = require('fs'); fs.cpSync('frontend/dist', 'dist', {recursive: true});\"",
    "start": "concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
